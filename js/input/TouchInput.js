// Constructor
Community.TouchInput = function(x, y){
	this.x = x || 0;

	var self = this;
};

// Restore constructor
Community.TouchInput.prototype.constructor = Community.ToucheInput;

// Prototype : method
Community.TouchInput.prototype = {

	hello: function() {
		alert("Quentin est beau");
	},

	louis: function(name) {
		alert("Salut " + name);
	}

};

/**
 * plugin :
 *
 * installer package control
 * JSFormat
 * DocBlockr
 * 
 */
